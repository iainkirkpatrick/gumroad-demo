<div class="py-16 flex flex-col">
  <div class="flex border border-black rounded-md">
    <div class="flex flex-col grow border-r border-black">
      <div class="py-8 px-6 flex flex-col border-b border-black">
        <h1 class="text-4xl"><%= @product[:name] %></h1>
      </div>
      <div class="flex items-center border-b border-black">
        <div class="p-6 flex flex-col items-center border-r border-black">
          <p>$<%= @product[:price_range] %></p>
        </div>
        <div class="p-6 flex flex-col items-center border-r border-black">
          <p><%= @user[:name] %></p>
        </div>
        <div class="p-6 flex flex-col items-center">
          <p>0 ratings</p>
        </div>
      </div>
      <div class="p-6 flex items-center">
        <p><%= @product[:description] %></p>
      </div>
    </div>
    <div class="p-6 flex flex-col">
      <%= form_with url: cart_path(@cart), method: :patch, local: true do |form| %>
        <input type="hidden" name="cart[product_public_id]" value="<%= @product[:public_id] %>" />
        <input type="hidden" name="cart[add_product]" value="true" />
        <button
          type="submit"
          class="px-4 py-3 min-w-72 border border-black rounded-md bg-[#FF90E8]"
        >Add to cart</button>
      <% end %>
    </div>
  </div>
</div>